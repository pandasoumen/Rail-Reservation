
// let boardingStation = localStorage(function (boarding_station){});

// getElementby(boardingStation);

// const dayofYear = (date) =>
//     Math.floor((date - new Date(date.getFullYear(), 0, 0)) / 1000 / 60 / 60 / 24);
// dayofYear(new Date());
// console.log();